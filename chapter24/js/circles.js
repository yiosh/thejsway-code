module.exports.getCircumference = radius => `The circumference of a circle of radius ${radius} is ${(2 * radius) * 3.14}`;
module.exports.getArea = radius => `Its area is ${(radius * radius) * 3.14}`;